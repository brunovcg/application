import styled from 'styled-components';

const StyledUsers = styled.div`
  .im-user-list {
    .im-section-content {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }
  }
  .im-users-status-filter {
    display: flex;
  }
`;

export default StyledUsers;
