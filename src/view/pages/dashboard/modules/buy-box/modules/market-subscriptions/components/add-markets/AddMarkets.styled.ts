import styled from 'styled-components';

const StyledAddMarkets = styled.div`
  .im-customer-subscriptions-add-market {
    display: flex;
    flex-wrap: wrap;
    gap: 15px;
    min-height: 70px;
  }
`;

export default StyledAddMarkets;
