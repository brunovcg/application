import styled from 'styled-components';

const StyledFilterSubscriptions = styled.div`
  .im-subscriptions-filters-content {
    display: flex;
    flex-wrap: wrap;
    gap: 20px;
  }
`;

export default StyledFilterSubscriptions;
