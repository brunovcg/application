import styled from 'styled-components';

const StyledProcessStatus = styled.div`
  display: flex;
  flex-direction: column;
  gap: 10px;

  .im-process-status-filter {
    width: 100%;
    display: flex;
    flex-wrap: wrap;
    gap: 20px;
  }
`;

export default StyledProcessStatus;
