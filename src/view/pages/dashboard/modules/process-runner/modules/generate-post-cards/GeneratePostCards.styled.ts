import styled from 'styled-components';

const StyledGeneratePostCards = styled.div`
  .im-generate-postcards-section {
    display: flex;
    flex-direction: column;
    gap: 20px;
    .im-generate-postcards-filters {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      gap: 20px;
    }
  }
`;

export default StyledGeneratePostCards;
