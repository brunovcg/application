import styled from 'styled-components';

const StyledGenerateSkipTraceRequest = styled.div`
  .im-generate-skip-trace-request-section {
    display: flex;
    flex-direction: column;
    gap: 20px;
    .im-generate-postcards-filters {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      gap: 20px;
    }
  }
`;

export default StyledGenerateSkipTraceRequest;
