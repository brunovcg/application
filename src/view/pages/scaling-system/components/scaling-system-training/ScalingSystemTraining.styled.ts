import styled from 'styled-components';

const StyledScalingSystemTraining = styled.div`
  .im-signature-solution-container {
    width: 100%;
    .im-signature-solution-content {
      margin-top: 20px;
      width: 100%;
    }
    .im-signature-solution-no-data {
      margin: 0 auto;
      width: 100%;
      max-width: 600px;
      border: 1px solid var(--border-color);
      border-radius: var(--container-border-radius);
      box-shadow: var(--container-box-shadow);
      img {
        max-width: 100%;
        border-radius: var(--container-border-radius);
      }
    }
  }
`;

export default StyledScalingSystemTraining;
