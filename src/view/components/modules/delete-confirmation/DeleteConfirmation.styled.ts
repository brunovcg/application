import styled from 'styled-components';

const StyledDeleteConfirmation = styled.div`
  width: 180px;
  .im-confirmation-state {
    width: 100%;
    gap: 15px;
    display: flex;
    flex-direction: row;
    justify-content: center;
  }
`;

export default StyledDeleteConfirmation;
