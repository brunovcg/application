import styled from 'styled-components';

const StyledUpdatePassword = styled.div`
  .im-update-password-form {
    width: 100%;
    display: flex;
    gap: 30px;
  }
`;

export default StyledUpdatePassword;
