import { Address } from '../../../apis/services/address-services/Address.services.types';
import { DialogButtons } from '../../components/modules/dialog/Dialog.types';

export type AddressDetailDialogTypes = { data: Address; buttons: DialogButtons };
