import styled from 'styled-components';

const StyledEditGroupDialog = styled.div`
  flex: 1;
  width: 100%;
  overflow-y: auto;

  .im-edit-group-dialog-message {
    margin-bottom: 20px;
  }
`;

export default StyledEditGroupDialog;
