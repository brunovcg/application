import styled from 'styled-components';

const StyledAddAdAudienceConfigurationDialog = styled.div`
  width: 100%;

  .im-add-ad-sense-configuration-form {
    display: flex;
    flex-direction: row;
    gap: 20px;
    flex-wrap: wrap;

    .im-selector {
      flex: 1;
    }
  }
`;

export default StyledAddAdAudienceConfigurationDialog;
