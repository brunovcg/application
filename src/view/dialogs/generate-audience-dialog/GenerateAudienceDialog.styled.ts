import styled from 'styled-components';

const StyledGenerateAudienceDialog = styled.div`
  width: 100%;

  .im-generate-audience-form {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }
`;

export default StyledGenerateAudienceDialog;
