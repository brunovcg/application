import styled from 'styled-components';

const StyledAccountSecurityDialog = styled.div`
  display: flex;
  flex-direction: column;
  gap: 10px;
`;

export default StyledAccountSecurityDialog;
