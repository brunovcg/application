import styled from 'styled-components';

const StyledUpdateMotivationDialog = styled.div`
  width: 100%;

  .im-update-motivation-dialog-form {
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    gap: 20px;
  }
`;

export default StyledUpdateMotivationDialog;
